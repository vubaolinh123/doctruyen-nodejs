/**
 * Định nghĩa các hooks cho Bookmark model
 * @param {Object} schema - Schema của Bookmark model
 */
const setupHooks = (schema) => {
  // Hiện tại không có hooks nào cần thiết lập
  // Nhưng chúng ta vẫn giữ file này để đồng bộ với cấu trúc
};

module.exports = setupHooks;
